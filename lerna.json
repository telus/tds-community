{
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md", "**/*.spec.jsx", "**/*.snap"],
      "message": "chore(publish): update packages"
    }
  },
  "packages": ["packages/**", "shared/**"]
}
