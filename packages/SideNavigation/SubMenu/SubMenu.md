`SideNavigation.SubMenu` contains a title and a list of links. Links can be in-page or route to other pages.

### Usage Criteria

- Use `SideNavigation.SubMenu` with large pages that have multiple sections
